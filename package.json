{
  "name": "restify-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix shared && npm install --prefix backend && npm install --prefix frontend",
    "db:up": "docker compose up -d mongodb",
    "db:down": "docker compose stop mongodb",
    "db:remove": "docker compose down -v --remove-orphans",
    "db:logs": "docker compose logs mongodb",
    "db:backup": "node ./scripts/docker-backup.mjs",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:remove": "docker compose down -v --remove-orphans",
    "docker:logs": "docker compose logs -f app mongodb",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix shared run build && npm --prefix backend run build && npm --prefix frontend run build",
    "test": "npm --prefix backend run test && npm --prefix frontend run test",
    "typecheck": "npm --prefix shared run typecheck && npm --prefix backend run typecheck && npm --prefix frontend run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.8.2"
  }
}
